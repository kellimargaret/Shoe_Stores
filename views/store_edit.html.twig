<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <title>Your Favorite Shoes</title>
</head>
<body>
    <div class="container">
        <h1 class>{{ store.getStoreName }}</h1>
            <form action="/stores/{{ store.getId }}" method="post">
                <p>Rename Store:</p>
                <input name="_method" type="hidden" value="patch">
                <div class="input-group">
                    <input id="store_name" name="store_name" type="text" class="form-control" placeholder="Happy Lemon" REQUIRED>
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="submit">Update</button>
                    </span>
                </div>
            </form>

            <form action='/stores/{{ store.getId }}' method='post'>
                <input name='_method' type='hidden' value='delete'>
                <button type='submit' class='btn btn-danger'>Delete Store</button>
            </form>
    </div>
</body>
</html>
